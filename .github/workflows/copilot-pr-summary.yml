name: Copilot PR Summary

on:
  pull_request:
    types:
      - opened
      - ready_for_review
      - reopened

permissions:
  contents: read
  pull-requests: write

jobs:
  summarize:
    if: >
      github.event.pull_request.draft == false &&
      github.event.pull_request.head.repo.full_name == github.repository

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v7

      - name: Install Copilot CLI
        run: npm install -g @github/copilot

      - name: Detect test files
        id: tests
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          if git diff --name-only "$BASE_SHA...$HEAD_SHA" \
            | grep -E '(^|/)(tests?/|.*(Test|Spec)\.(php|js|ts|tsx)|.*\.(test|spec)\.(js|ts|tsx))' >/dev/null; then
            echo "has_tests=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_tests=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Create PR diff
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          git diff \
            --unified=1 \
            "$BASE_SHA...$HEAD_SHA" \
            > pr-diff.txt

          head -c 20000 pr-diff.txt > pr-diff-limited.txt

      - name: Check diff
        id: diff
        run: |
          if [ -s pr-diff-limited.txt ]; then
            echo "has_diff=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_diff=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Create prompt
        if: steps.diff.outputs.has_diff == 'true'
        env:
          HAS_TESTS: ${{ steps.tests.outputs.has_tests }}
        run: |
          if [ "$HAS_TESTS" = "true" ]; then
            TEST_SECTION=$'### テスト\n- 追加・変更されたテスト内容\n- ローカル環境にて動作確認済み'
          else
            TEST_SECTION=$'### テスト\n- なし\n- ローカル環境にて動作確認済み'
          fi

          cat <<EOF > prompt.txt
          以下はPull RequestのGit差分です。

          レビュー担当者が変更内容を短時間で理解できるよう、
          日本語で簡潔に要約してください。

          ### 実装内容
          - 変更内容

          ### 主な変更ファイル
          - `ファイル名`: 変更内容

          ${TEST_SECTION}

          ルール:
          - Git差分から確認できる事実だけを書く
          - 推測しない
          - 最大10項目
          - Markdownのみ出力
          - 前置きを書かない

          ## Git diff
          EOF

          cat pr-diff-limited.txt >> prompt.txt

      - name: Generate PR summary
        id: copilot
        if: steps.diff.outputs.has_diff == 'true'
        env:
          COPILOT_GITHUB_TOKEN: ${{ secrets.COPILOT_TOKEN }}
        run: |
          copilot \
            --yolo \
            -p "$(cat prompt.txt)" \
            --max-ai-credits 35 \
            > generated-summary.md

      - name: Check generated summary
        id: summary
        run: |
          if [ -s generated-summary.md ]; then
            echo "has_summary=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_summary=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Get current PR body
        if: steps.summary.outputs.has_summary == 'true'
        env:
          GITHUB_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          gh pr view "$PR_NUMBER" \
            --json body \
            --jq '.body' > current-body.md

      - name: Build new PR body
        if: steps.summary.outputs.has_summary == 'true'
        run: |
          python3 <<'PY'
          import re

          with open("current-body.md", encoding="utf-8") as f:
              body = f.read()

          with open("generated-summary.md", encoding="utf-8") as f:
              summary = f.read().strip()

          start = "<!-- copilot-summary:start -->"
          end = "<!-- copilot-summary:end -->"

          block = f"""## Copilotによる変更概要

          {start}

          {summary}

          {end}"""

          pattern = re.compile(
              r"## Copilotによる変更概要.*?"
              r"<!-- copilot-summary:end -->",
              re.DOTALL,
          )

          if pattern.search(body):
              body = pattern.sub(block, body)
          else:
              body = body.rstrip() + "\n\n" + block + "\n"

          with open("new-body.md", "w", encoding="utf-8") as f:
              f.write(body)
          PY

      - name: Update PR body
        if: steps.summary.outputs.has_summary == 'true'
        env:
          GITHUB_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          gh pr edit "$PR_NUMBER" \
            --body-file new-body.md
